#include <stdio.h>
#include "3-calc.h"
/**
*
*
*
*/
int op_add (int a, int b)
{
int op_add(int a, int b);
int op_sub(int a, int b);
int op_mul(int a, int b);
int op_div(int a, int b);
int op_mod(int a, int b);
int a;
int b;
int sum, sub, mul;
float mod;
sum = op_add(%d + %d = %d, a, b, a + b);
sub = op_sub(%d - %d = %d, a, b, a - b);
mul = op_mul(%d * %d = %d, a, b, a * b);
mod = op_mod(%d % %d = %f, a, b, a % b);
return ();
}
