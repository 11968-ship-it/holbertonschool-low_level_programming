#include <stdlib.h>
/*for malloc, free*/
#include <string.h>
/*for strdup, strlen*/
#include "lists.h"
#include "lists.h"
/**
*/
list_t *add_node_end(list_t **head, const char *str)
{
list_t *new_node = malloc(sizeof(list_t));
}
