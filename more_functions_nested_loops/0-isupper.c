#include "main.h"

/**
 * int _isupper(int c) - checks for uppercase character
 */
int _isupper(char c)
{
char ch = 'c';
if (_isupper(ch))
printf("%c is uppercase.\n", ch);
else
{
printf("%c is not uppercase.\n", ch);
}
return (0);
}
